@using Microsoft.AspNetCore.Components.Rendering;
@typeparam T where T : IDiscovryCommonProperty
<h3>Details</h3>

@code {
    private void BtmFavoriRender(RenderTreeBuilder __builder)
    {
        if (Item !=null)
        {
            <BtmFav Item="Item"></BtmFav>
        }       

    }
}
